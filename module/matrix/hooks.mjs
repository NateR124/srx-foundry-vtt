import { calculateNoise, getActiveIC } from "../rules/matrix.mjs";

/**
 * Register Matrix hooks.
 */
export function registerMatrixHooks() {
  // Hooks for matrix OS tracking, IC auto-firing, etc. will go here.
}
